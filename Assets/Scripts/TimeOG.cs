using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimeOG : MonoBehaviour
{
    public float timeValue = 90;

    // Update is called once per frame
    void Update()
    {
        timeValue -= Time
    }
}
